function aTVec = oneShot(PDIx0)
    % Incoming x0 is nondim, and at PDI
    % Returns vector of aT values, as aT is dependent on time only
    % This aT is not very accurate, especially early on as the landing site
    % gravity is assumed for everywhere during this flight

end

